rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Lock down Firestore completely - ALL access through Cloud Functions only
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
